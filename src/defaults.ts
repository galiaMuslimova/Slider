import { IDefaults } from './interfaces/interfaces';

const defaults: IDefaults = {
  min: 10,
  max: 40,
  step: 4,
  from: 8,
  to: 24,
  isVertical: false,
  hasTip: true,
  withRange: true,
};

export default defaults;
